String dWrod="zebra";
String dCanonical = toCanonical(dWord);//"aberz"
static String ToCanonical(String s){
char[] letters = dWord.toCharArray();//[z][e][b][r][a]
Arrays.sort(letters);//letters will be like [a][b][e][r][z]
return new String(leters);
}

ArrayList<String> wordList;

Collections(wordList);